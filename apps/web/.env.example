# Copy to apps/web/.env.local (never commit real secrets)

# Dev proxy target for Vite (default is usually fine)
# VITE_PROXY_TARGET=http://127.0.0.1:8000

# Optional: send X-API-Key when API_KEY is set on the backend
# VITE_API_KEY=
